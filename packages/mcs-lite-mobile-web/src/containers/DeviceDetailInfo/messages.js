import { defineMessages } from 'react-intl';

const messages = defineMessages({
  'DeviceDetailInfo.deviceIntro': {
    id: 'DeviceDetailInfo.deviceIntro',
    description: 'Title',
    defaultMessage: '測試裝置介紹',
  },
});

export default messages;
