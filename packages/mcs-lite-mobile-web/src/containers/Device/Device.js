import React from 'react';
import { Button, DataChannelCard, DataChannel } from 'mcs-lite-ui';

const Device = () =>
  <div>
    Device page
    <Button>mcs-lite button</Button>
  </div>

export default Device;
