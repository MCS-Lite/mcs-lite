import { theme } from 'mcs-lite-theme';

const landingTheme = {
  ...theme,
  color: {
    ...theme.color,
    black: '#40494D',
    grayBase: '#75868C',
  },
  base: {
    ...theme.base,
    bodyColor: '#40494D',
  },
};

export default landingTheme;
