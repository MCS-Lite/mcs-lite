{
  "name": "mcs-lite-design",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf lib",
    "prebuild": "npm run clean",
    "build": "svgo -f 'src/icon' -o lib/icon --pretty --multipass --indent=2 --quiet",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "ISC"
}
