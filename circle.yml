machine:
  node:
    version: 7.5.0

dependencies:
  override:
    - npm install

  cache_directories:
    - node_modules

test:
  override:
    - ./tasks/test.sh

general:
  artifacts:
    - "packages/mcs-lite-theme"
    - "packages/mcs-lite-ui"
    - "packages/mcs-lite-icon"
    - "packages/mcs-lite-mobile-web/mcs-lite-mobile-web.pot"
